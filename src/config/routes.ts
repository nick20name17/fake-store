export const routes = {
    home: '/',
    users: '/users',
    products: '/products',
    cart: '/cart',
    login: '/login'
}

export const publicRoutes = [routes.home, routes.login, routes.products]
